using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Text;
using System.Threading.Tasks;

namespace CSharp_Zero_to_Hero.Chapter9_Operator_Logical
{
    public class LogicalOperator
    {
        //public static void  Main()
        //{
        //    //Writte a Program to take input(Math/Science Marks) from user and retturn the result passing both , pass in one,failboth
        //    //pass marks is 35

        //    double maths, science;
        //    Console.WriteLine("Enter Maths Marks");
        //    maths=Convert.ToDouble(Console.ReadLine());
        //    Console.WriteLine("ENter Science Marks");
        //    science = Convert.ToDouble(Console.ReadLine());
        //    bool passboth = (maths >= 35 && science >= 35);
        //    Console.WriteLine($"Passed in both {passboth}");
        //    bool passone = (maths >= 35 || science >= 35);
        //    Console.WriteLine($"pass in one {passone}");
        //    bool failedboth = !(passone);
        //    Console.WriteLine($"failed both {failedboth}");
        //}
    }
}
